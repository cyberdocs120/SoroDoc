apiVersion: v1
kind: Service
metadata:
  name: {{ include "sorodoc.fullname" . }}
  labels:
    {{- include "sorodoc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sorodoc.selectorLabels" . | nindent 4 }}

SoroDoc has been deployed successfully!

{{- if .Values.ingress.enabled }}
Access the API via ingress:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
Get the application URL by running:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "sorodoc.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl port-forward $POD_NAME 3001:{{ .Values.service.port }}
  echo "Visit http://127.0.0.1:3001"
{{- end }}

Health check:
  curl http://<service-ip>:{{ .Values.service.port }}/health

{
  "name": "sorodoc-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "AI-Powered Documentation, SDK & API Reference Generator for Stellar / Soroban Smart Contracts",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --parallel",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0",
    "prettier": "^3.4.0"
  },
  "packageManager": "npm@11.6.2",
  "engines": {
    "node": ">=18.0.0"
  }
}
